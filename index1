<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Free online image compression tool to optimize images with customizable compression levels. Reduce file size without losing quality.">
    <meta name="keywords" content="image compression, optimize image, reduce image size, free image compressor, web image optimizer">
    <meta name="author" content="Your Name">
    <meta name="robots" content="index, follow">
    <title>Image Compressor - Optimize Images Online</title>
    <link rel="icon" type="image/png" href="favicon.png">
    <meta property="og:title" content="Image Compressor - Optimize Images Online">
    <meta property="og:description" content="Compress images online with customizable quality settings. Fast, free, and easy to use.">
    <meta property="og:image" content="https://yourwebsite.com/og-image.jpg">
    <meta property="og:url" content="https://yourwebsite.com">
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-XXXXXXXXXXXXXXXX"
            crossorigin="anonymous"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            color: #fff;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            min-height: 100vh;
            padding: 20px;
        }

        header {
            text-align: center;
            padding: 40px 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            margin-bottom: 20px;
        }

        header h1 {
            font-size: 2.8rem;
            font-weight: 700;
            color: #fff;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

        header p {
            font-size: 1.2rem;
            color: #d1e8ff;
            opacity: 0.9;
        }

        main {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .compressor-tool {
            flex: 1;
            min-width: 300px;
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .compressor-tool:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2);
        }

        .compressor-tool h2 {
            font-size: 1.8rem;
            color: #1e3c72;
            margin-bottom: 20px;
            text-align: center;
        }

        input[type="file"] {
            display: block;
            margin: 0 auto;
            padding: 10px;
            background: #f1f4ff;
            border: 2px dashed #2a5298;
            border-radius: 10px;
            width: 100%;
            max-width: 400px;
            cursor: pointer;
            transition: border-color 0.3s ease;
        }

        input[type="file"]:hover {
            border-color: #007BFF;
        }

        .compression-options {
            margin: 20px 0;
            text-align: center;
        }

        .compression-options label {
            font-size: 1.2rem;
            color: #1e3c72;
            font-weight: 500;
        }

        #quality {
            width: 100%;
            max-width: 400px;
            margin: 15px auto;
            accent-color: #007BFF;
        }

        #qualityValue {
            font-weight: 600;
            color: #007BFF;
            font-size: 1.1rem;
        }

        button {
            display: block;
            margin: 0 auto;
            background: linear-gradient(135deg, #007BFF, #00c6ff);
            color: #fff;
            padding: 12px 30px;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            font-size: 1.1rem;
            font-weight: 500;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        button:disabled {
            background: #ccc;
            cursor: not-allowed;
        }

        button:hover:not(:disabled) {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0, 123, 255, 0.4);
        }

        #preview {
            margin-top: 20px;
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            justify-content: center;
        }

        #preview h3 {
            width: 100%;
            text-align: center;
            color: #1e3c72;
            margin-bottom: 10px;
        }

        #preview img {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        #preview img:hover {
            transform: scale(1.02);
        }

        #result {
            margin-top: 20px;
            text-align: center;
            color: #1e3c72;
        }

        #result.hidden {
            display: none;
        }

        #downloadLink {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background: #007BFF;
            color: #fff;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 500;
            transition: background 0.3s ease;
        }

        #downloadLink:hover {
            background: #0056b3;
        }

        .ad-container, .ad-sidebar {
            width: 100%;
            max-width: 300px;
            margin: 20px auto;
            border-radius: 10px;
            overflow: hidden;
        }

        .ad-sidebar {
            flex: 0 0 300px;
        }

        footer {
            text-align: center;
            padding: 20px 0;
            margin-top: 40px;
            color: #d1e8ff;
        }

        footer nav a {
            color: #00c6ff;
            margin: 0 10px;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        footer nav a:hover {
            color: #fff;
        }

        @media (max-width: 768px) {
            main {
                flex-direction: column;
                align-items: center;
            }

            .ad-sidebar {
                max-width: 100%;
            }

            header h1 {
                font-size: 2rem;
            }

            header p {
                font-size: 1rem;
            }

            .compressor-tool {
                padding: 20px;
            }

            button {
                padding: 10px 20px;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Image Compressor</h1>
        <p>Optimize your images with customizable compression levels</p>
    </header>

    <main>
        <div class="ad-container">
            <ins class="adsbygoogle"
                 style="display:block"
                 data-ad-client="ca-pub-XXXXXXXXXXXXXXXX"
                 data-ad-slot="YOUR_AD_SLOT_ID"
                 data-ad-format="auto"
                 data-full-width-responsive="true"></ins>
            <script>
                (adsbygoogle = window.adsbygoogle || []).push({});
            </script>
        </div>

        <section class="compressor-tool">
            <h2>Compress Your Image</h2>
            <input type="file" id="imageInput" accept="image/*" aria-label="Select image to compress">
            <div class="compression-options">
                <label for="quality">Compression Level:</label>
                <input type="range" id="quality" min="10" max="100" value="80" aria-label="Compression quality slider">
                <span id="qualityValue">80%</span>
            </div>
            <button id="compressBtn" disabled>Compress Image</button>
            <div id="preview">
                <h3>Preview</h3>
                <img id="originalPreview" alt="Original image preview">
                <img id="compressedPreview" alt="Compressed image preview">
            </div>
            <div id="result" class="hidden">
                <p>Original Size: <span id="originalSize"></span></p>
                <p>Compressed Size: <span id="compressedSize"></span></p>
                <a id="downloadLink" href="#" download="compressed-image.jpg">Download Compressed Image</a>
            </div>
        </section>

        <aside class="ad-sidebar">
            <ins class="adsbygoogle"
                 style="display:block"
                 data-ad-client="ca-pub-XXXXXXXXXXXXXXXX"
                 data-ad-slot="YOUR_AD_SLOT_ID"
                 data-ad-format="auto"
                 data-full-width-responsive="true"></ins>
            <script>
                (adsbygoogle = window.adsbygoogle || []).push({});
            </script>
        </aside>
    </main>

    <footer>
        <p>© 2025 Image Compressor. All rights reserved.</p>
        <nav>
            <a href="/privacy-policy">Privacy Policy</a> |
            <a href="/terms-of-service">Terms of Service</a>
        </nav>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const imageInput = document.getElementById('imageInput');
            const qualitySlider = document.getElementById('quality');
            const qualityValue = document.getElementById('qualityValue');
            const compressBtn = document.getElementById('compressBtn');
            const originalPreview = document.getElementById('originalPreview');
            const compressedPreview = document.getElementById('compressedPreview');
            const originalSize = document.getElementById('originalSize');
            const compressedSize = document.getElementById('compressedSize');
            const downloadLink = document.getElementById('downloadLink');
            const result = document.getElementById('result');

            let originalImage = null;

            qualitySlider.addEventListener('input', () => {
                qualityValue.textContent = `${qualitySlider.value}%`;
            });

            imageInput.addEventListener('change', () => {
                if (imageInput.files.length > 0) {
                    compressBtn.disabled = false;
                    const file = imageInput.files[0];
                    originalImage = file;
                    originalSize.textContent = `${(file.size / 1024).toFixed(2)} KB`;
                    originalPreview.src = URL.createObjectURL(file);
                } else {
                    compressBtn.disabled = true;
                    originalPreview.src = '';
                }
            });

            compressBtn.addEventListener('click', async () => {
                if (!originalImage) return;

                const quality = qualitySlider.value / 100;
                const canvas = document.createElement('canvas');
                const ctx = canvas.getContext('2d');
                const img = new Image();

                img.src = URL.createObjectURL(originalImage);
                await new Promise((resolve) => (img.onload = resolve));

                canvas.width = img.width;
                canvas.height = img.height;
                ctx.drawImage(img, 0, 0);

                const compressedDataUrl = canvas.toDataURL('image/jpeg', quality);
                compressedPreview.src = compressedDataUrl;

                const compressedBlob = await fetch(compressedDataUrl).then((res) => res.blob());
                compressedSize.textContent = `${(compressedBlob.size / 1024).toFixed(2)} KB`;

                downloadLink.href = compressedDataUrl;
                result.classList.remove('hidden');
            });
        });
    </script>
</body>
</html>
